# Copy to `.env` for local macOS packaging (`.env` is gitignored).
# Find identities with: security find-identity -v -p codesigning
#
# Apple Development is enough for local notification testing.
# Release/CI should use Developer ID via APPLE_SIGNING_IDENTITY secret.
APPLE_SIGNING_IDENTITY=Apple Development: Your Name (TEAMID)
